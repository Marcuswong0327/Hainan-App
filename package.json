{
  "name": "hainan-app",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Hainan Association app — monorepo: frontend, backend (Edge Functions), database (Supabase)",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "sync:functions": "node scripts/sync-functions.js",
    "db:push": "npm run sync:functions && cd database && supabase db push",
    "functions:deploy": "npm run sync:functions && cd database && supabase functions deploy",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:dev": "docker compose -f docker-compose.dev.yml up --build"
  }
}
