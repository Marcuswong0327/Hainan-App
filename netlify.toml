# Monorepo: the Vite app lives in frontend/
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback — serve index.html for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
